<header>
  <nav>
    <!-- Logo R+M -->
    <a href="/" class="logo">
      <span class="circle">
        <span class="rm">R<span class="divider">•</span>M</span>
      </span>
    </a>
    <!-- Menú elegante con tu paleta -->
    <div class="internal-links">
      <a href="/">Inicio</a>
      <a href="/blog">Blog</a>
      <a href="/about">Sobre mí</a>
      <a href="/contact">Contacto</a>
    </div>
  </nav>
</header>

<style>
  header {
    background-color: #081534; /* Fondo principal */
    padding: 1.2em;
    box-shadow: 0 4px 20px rgba(0, 240, 240, 0.1);
  }
  nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    max-width: 1200px;
    margin: 0 auto;
  }
  .logo .circle {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 70px;
    height: 70px;
    border-radius: 50%;
    border: 3px solid #00F0F0; /* Neon destacado */
    box-shadow: 0 0 20px rgba(0, 240, 240, 0.6);
  }
  .logo .rm {
    font-weight: bold;
    color: #F2F2F2; /* Texto principal */
    font-size: 1.6rem;
    letter-spacing: 0.05em;
    text-shadow: 0 0 10px #00F0F0;
  }
  .divider {
    color: #00F0F0; /* Neon */
    margin: 0 0.1em;
    text-shadow: 0 0 10px #00F0F0;
  }
  .internal-links {
    display: flex;
    gap: 2em;
  }
  .internal-links a {
    color: #F2F2F2; /* Texto principal */
    font-weight: bold;
    font-size: 1.2rem;
    text-decoration: none;
    padding: 0.8em 1.8em;
    background-color: #00607A; /* Tarjetas / secciones */
    border: 2px solid #00F0F0; /* Neon borde */
    border-radius: 50px;
    transition: all 0.4s ease;
    box-shadow: 0 0 15px rgba(0, 240, 240, 0.3);
  }
  .internal-links a:hover {
    color: #081534; /* Fondo oscuro para contraste */
    background-color: #00F0F0; /* Neon full al hover */
    box-shadow: 0 0 30px rgba(0, 240, 240, 0.8);
    transform: translateY(-3px);
  }
  /* Responsive móvil */
  @media (max-width: 720px) {
    nav {
      flex-direction: column;
      text-align: center;
    }
    .internal-links {
      margin-top: 1.5em;
      gap: 1em;
      flex-wrap: wrap;
      justify-content: center;
    }
    .internal-links a {
      font-size: 1.1rem;
      padding: 0.7em 1.5em;
    }
  }
</style>
