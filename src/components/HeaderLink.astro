---
import HeaderLink from './HeaderLink.astro';
import { SITE_TITLE } from '../consts';
---

<header>
  <nav>
    <!-- Logo R+M -->
    <a href="/" class="logo">
      <span class="circle">
        <span class="rm">R<span class="divider">•</span>M</span>
      </span>
    </a>

    <!-- Navegación -->
    <div class="internal-links">
      <HeaderLink href="/">Inicio</HeaderLink>
      <HeaderLink href="/blog">Blog</HeaderLink>
      <HeaderLink href="/sobre-mi">Sobre mí</HeaderLink>
      <HeaderLink href="/contacto">Contacto</HeaderLink>
    </div>

    <!-- Social Links (opcional) -->
    <div class="social-links">
      <a href="https://twitter.com/astrodotbuild" target="_blank">
        <span class="sr-only">Follow Astro on Twitter</span>
        <!-- Icono SVG Twitter -->
        <svg viewBox="0 0 16 16" aria-hidden="true" width="24" height="24">
          <path fill="currentColor" d="M5.026 15c6.038 0 9.341-5.003 9.341-9.334..."></path>
        </svg>
      </a>
    </div>
  </nav>
</header>

<style>
  header {
    background-color: rgb(245,245,245); /* fondo blanco grisáceo */
    padding: 0.5em 1em;
    box-shadow: 0 2px 8px rgba(17,17,17,0.05);
  }

  nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
  }

  /* Logo estilo obturador */
  .logo .circle {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 50px;
    height: 50px;
    border-radius: 50%;
    border: 2px solid #0aaaff;
  }

  .logo .rm {
    font-weight: bold;
    color: #0aaaff;
    font-size: 1.2rem;
    letter-spacing: 0.05em;
  }

  .divider {
    color: #0aaaff;
    margin: 0 0.1em;
  }

  .internal-links {
    display: flex;
    gap: 1em;
  }

  .internal-links a {
    color: rgb(17,17,17);
    font-weight: 600;
    text-decoration: none;
    padding: 0.5em;
    transition: 0.3s;
  }

  .internal-links a.active,
  .internal-links a:hover {
    color: #0aaaff;
    border-bottom: 2px solid #0aaaff;
  }

  .social-links {
    display: flex;
    gap: 0.5em;
  }

  .social-links svg {
    fill: #0aaaff;
    transition: 0.3s;
  }

  .social-links svg:hover {
    fill: #0077cc;
  }

  /* Responsive móvil */
  @media (max-width: 720px) {
    .internal-links {
      flex-basis: 100%;
      justify-content: center;
      margin-top: 0.5em;
      flex-wrap: wrap;
    }
    .social-links {
      display: none;
    }
  }
</style>
